<% if flash[:alert] %>
    <div class="notice"><%= flash[:alert] %></div>
  <% end %>

<h1>Edit your course</h1>

<%= render "form", event: @event %>
<!-- Render the partial for the create / edit course form. Would as well work with just rendering "form" -->
